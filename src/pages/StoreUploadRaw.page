<apex:page standardController="Store__c" contentType="application/vnd.ms-excel">
    <apex:pageBlock >
        <apex:pageBlockTable value="{!Store__c.raw_material__r}" var="raw">
            <apex:column value="{!raw.Name}"/>
            <apex:column value="{!raw.Amount__c}"/>
            <apex:column value="{!raw.Made_in__c}"/>
            <apex:column value="{!raw.Quality__c}"/>
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>